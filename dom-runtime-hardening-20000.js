(()=>{
  const failures=[];let checks=0,seed=0x6d6f646d;
  const rnd=()=>{seed^=seed<<13;seed^=seed>>>17;seed^=seed<<5;return((seed>>>0)/4294967296)};
  const test=(name,ok,detail='')=>{checks++;if(!ok&&failures.length<25)failures.push({name,detail})};
  const requiredGlobals=['DOMObservationModel','DOMHazardTaxonomy','DOMEnvironmentalFusion','DOMEnvironmentalDefense','DOMPlanetHealth','DOMPlanetaryEcosystemDefense','DOMPublicSensorRegistry','DOMSensorActivation','DOMGlobalSensorGlobe','DOMSHazardResearch','DOMSHazardVoice'];
  for(const name of requiredGlobals)test(`global:${name}`,!!window[name]);
  test('global compatibility bridge',!!window.DOMGlobalCompat&&window.DOMGlobalCompat.ok===true&&window.DOMGlobalCompat.count>=11);
  const requiredIds=['events','map','sourceState','refreshAll','enableLocal','voiceOn','voiceMute','voiceState','planetHealthSidebar'];for(const id of requiredIds)test(`dom:${id}`,!!document.getElementById(id));
  if(typeof esc==='function')test('HTML quote escape terminated',esc('"')==='&quot;');else test('HTML escape function available',false);
  if(typeof safeUrl==='function'){test('javascript URL rejected',safeUrl('javascript:alert(1)')==='#');test('https URL retained',/^https:/.test(safeUrl('https://example.com/x')))}else test('safe URL function available',false);
  if(window.DOMSensorActivation){for(let i=0;i<5000;i++){const x=rnd()*3-1,a=DOMSensorActivation.activation({freshness:x,quality:1-x,anomaly:rnd(),persistence:rnd(),corroboration:rnd(),hazardCoupling:rnd()});test('activation bounded',Number.isFinite(a.score)&&a.score>=0&&a.score<=1)}}else for(let i=0;i<5000;i++)test('activation module missing',false);
  if(window.DOMGlobalSensorGlobe){for(let i=0;i<5000;i++){const lat=rnd()*220-110,lon=rnd()*440-220,s=DOMGlobalSensorGlobe.normalizeSensor({id:String(i),lat,lon,activation:rnd()});const sourceValid=lat>=-90&&lat<=90&&lon>=-180&&lon<=180;test('coordinate validity preserved',sourceValid?(s.lat===lat&&s.lon===lon):(s.lat===null&&s.lon===null&&s.locationPrecision==='unresolved'))}}else for(let i=0;i<5000;i++)test('globe module missing',false);
  if(window.DOMGlobalSensorGlobe){for(let i=0;i<5000;i++){const alt=rnd()*30000,l=DOMGlobalSensorGlobe.lodForAltitudeKm(alt);test('LOD bounded',Number.isInteger(l.level)&&l.level>=0&&l.level<=5&&Number.isFinite(l.maxSensors)&&l.maxSensors>0)}}else for(let i=0;i<5000;i++)test('globe LOD module missing',false);
  if(window.DOMPlanetHealth){for(let i=0;i<5000;i++){const p=[];for(let j=0;j<5;j++)p.push({value:rnd()*1.4-.2,weight:rnd()*2,quality:rnd()*1.4-.2,freshness:rnd()*1.4-.2});const s=DOMPlanetHealth.weightedScore(p);test('planet weighted score bounded',s.value===null||(Number.isFinite(s.value)&&s.value>=0&&s.value<=1&&s.coverage>=0&&s.coverage<=1))}}else for(let i=0;i<5000;i++)test('planet health module missing',false);
  if(window.DOMGlobalSensorGlobe){const west=DOMGlobalSensorGlobe.viewportFilter([{lat:0,lon:179,activation:1},{lat:0,lon:-179,activation:1},{lat:0,lon:0,activation:1}],{west:170,east:-170});test('dateline viewport includes both sides',west.length===2)}
  if(typeof geometryCentroid==='function'){const c=geometryCentroid({type:'Polygon',coordinates:[[[179,10],[-179,10],[-179,12],[179,12],[179,10]]]});test('dateline alert centroid stays near dateline',!!c&&Math.abs(c.lon)>170)}
  if(window.DOMEnvironmentalDefense){const c=[{lat:0,valueC:20,quality:1,coverage:1}];const g=DOMEnvironmentalDefense.globalAreaWeightedMean(c);test('global temperature refuses unknown denominator',g.coverage===null&&g.coverageResolved===false)}
  if(window.DOMPlanetHealth){const three=[{t:2020,value:1,sigma:.1},{t:2021,value:2,sigma:.1},{t:2022,value:3,sigma:.1}];test('three-point future date refused',DOMPlanetHealth.forecastWindow({series:three,threshold:4}).kind==='unresolved');const m=DOMPlanetHealth.weightedScore([{value:.7,weight:1}]);test('missing quality metadata cannot claim full coverage',m.coverage<1)}
  if(window.DOMPlanetaryEcosystemDefense){const now=new Date().toISOString(),fresh=DOMPlanetaryEcosystemDefense.forestDisturbance({lat:0,lon:0,observedAt:now,confidence:.9,areaHa:100}),old=DOMPlanetaryEcosystemDefense.forestDisturbance({lat:0,lon:0,observedAt:'2000-01-01T00:00:00Z',confidence:.9,areaHa:100});test('ecosystem severity separate from freshness',Math.abs(fresh.severity-old.severity)<1e-12&&fresh.evidenceStrength>old.evidenceStrength)}
  const detail={ok:failures.length===0,checks,failures:failures.length,firstFailure:failures[0]?`${failures[0].name}${failures[0].detail?`: ${failures[0].detail}`:''}`:null,sampleFailures:failures};window.DOMRuntimeHardeningAudit=detail;try{window.dispatchEvent(new CustomEvent('dom:hardening-audit',{detail}))}catch(_){}if(detail.ok)console.info(`D.O.M. runtime hardening PASS · ${checks} invariant checks`);else console.warn('D.O.M. runtime hardening failures',detail);
})();