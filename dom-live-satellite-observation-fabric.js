(()=>{
'use strict';
const EUMET_WMS='https://view.eumetsat.int/geoserver/wms';
const NASA_GIBS='https://gibs.earthdata.nasa.gov/wmts/epsg3857/best';
const SOURCES=Object.freeze([
 {id:'goes-east',agency:'NOAA',satellite:'GOES-East',instrument:'ABI',cadenceMin:10,coverage:'Americas / Atlantic'},
 {id:'goes-west',agency:'NOAA',satellite:'GOES-West',instrument:'ABI',cadenceMin:10,coverage:'Americas / Pacific'},
 {id:'meteosat',agency:'EUMETSAT',satellite:'Meteosat',instrument:'FCI',cadenceMin:10,coverage:'Europe / Africa / Atlantic'},
 {id:'himawari',agency:'JMA',satellite:'Himawari-9',instrument:'AHI',cadenceMin:10,coverage:'Asia / Oceania / Western Pacific'},
 {id:'worldview',agency:'NASA EOSDIS',satellite:'polar-orbiting constellation',instrument:'VIIRS',cadenceMin:240,coverage:'global / polar reinforcement'}
]);
const state={bound:false,map:null,lastRefresh:null,sources:SOURCES.map(x=>({...x,status:'UNVERIFIED',truth:'COVERAGE_GAP',acquiredAt:null,ageMin:null})),timer:0,attachTimer:0,eumet:{layer:'mtg_fd:rgb_geocolour',mapLayer:'dom-satellite-meteosat',mapSource:'dom-satellite-meteosat-source',lastUrl:null,lastError:null},worldview:{mapLayer:'dom-satellite-worldview',mapSource:'dom-satellite-worldview-source',date:null,lastError:null}};
const emit=(name,detail)=>window.dispatchEvent(new CustomEvent(name,{detail}));
function freshness(acquiredAt,cadenceMin){if(!acquiredAt)return{status:'TIMESTAMP_PENDING',truth:'COVERAGE_GAP',ageMin:null};const t=Date.parse(acquiredAt);if(!Number.isFinite(t))return{status:'BAD_TIMESTAMP',truth:'COVERAGE_GAP',ageMin:null};const ageMin=Math.max(0,(Date.now()-t)/60000),fresh=ageMin<=Math.max(cadenceMin*3,45);return{ageMin,status:fresh?'OBSERVED':'STALE',truth:fresh?'OBSERVED':'COVERAGE_GAP'};}
function publish(){state.lastRefresh=new Date().toISOString();emit('dom:satellite-observation-fabric',{classification:'BACKGROUND',generatedAt:state.lastRefresh,sources:state.sources.map(x=>({...x}))});renderLedger();}
function ingest(meta){if(!meta||!meta.id)return false;const i=state.sources.findIndex(x=>x.id===meta.id);if(i<0)return false;const base=state.sources[i],f=freshness(meta.acquiredAt,base.cadenceMin);state.sources[i]={...base,...meta,...f,classification:'BACKGROUND'};publish();return f.truth==='OBSERVED';}
function markGap(id,reason){const i=state.sources.findIndex(x=>x.id===id);if(i<0)return;state.sources[i]={...state.sources[i],status:'COVERAGE_GAP',truth:'COVERAGE_GAP',gapReason:String(reason||'fresh authoritative observation unavailable'),acquiredAt:null,ageMin:null};publish();}
function dateUTC(offsetDays=0){const d=new Date(Date.now()+offsetDays*86400000);return d.toISOString().slice(0,10)}
function worldviewTiles(date){return `${NASA_GIBS}/VIIRS_SNPP_CorrectedReflectance_TrueColor/default/${date}/GoogleMapsCompatible_Level9/{z}/{y}/{x}.jpg`;}
function eumetUrl(){const q=new URLSearchParams({service:'WMS',version:'1.3.0',request:'GetMap',layers:state.eumet.layer,styles:'',format:'image/png',crs:'EPSG:4326',bbox:'-80,-80,80,80',width:'1600',height:'1600',transparent:'true'});q.set('_dom_refresh',String(Math.floor(Date.now()/600000)));return `${EUMET_WMS}?${q}`;}
function imageProbe(url){return new Promise((resolve,reject)=>{const im=new Image();im.crossOrigin='anonymous';im.onload=()=>resolve({width:im.naturalWidth,height:im.naturalHeight});im.onerror=()=>reject(new Error('satellite image request failed'));im.src=url;});}
function beforeHazards(m){for(const id of['dom-integrity-hazards-fill','dom-live-points-layer','dom-eira-quake-origin'])if(m.getLayer?.(id))return id;return undefined;}
function installWorldview(){const m=state.map;if(!m||!m.loaded?.())return false;const sid=state.worldview.mapSource,lid=state.worldview.mapLayer,date=dateUTC(-1);try{if(m.getLayer?.(lid))return true;if(m.getSource?.(sid))m.removeSource(sid);m.addSource(sid,{type:'raster',tiles:[worldviewTiles(date)],tileSize:256,maxzoom:9,attribution:'NASA EOSDIS GIBS / VIIRS'});m.addLayer({id:lid,type:'raster',source:sid,paint:{'raster-opacity':['interpolate',['linear'],['zoom'],0,.64,3,.60,6,.50,9,.30,11,0],'raster-saturation':-.06,'raster-contrast':.10,'raster-fade-duration':900}},beforeHazards(m));state.worldview.date=date;state.worldview.lastError=null;ingest({id:'worldview',acquiredAt:`${date}T23:59:59Z`,retrievedAt:new Date().toISOString(),product:'VIIRS_SNPP_CorrectedReflectance_TrueColor',rendered:true,status:'OBSERVED_DAILY_COMPOSITE',truth:'OBSERVED',temporalResolution:'daily; not real-time'});return true}catch(e){state.worldview.lastError=String(e?.message||e);return false;}}
async function refreshEumet(){const m=state.map;if(!m||!m.loaded?.())return false;const url=eumetUrl();try{const dims=await imageProbe(url);if(!dims.width||!dims.height)throw new Error('empty satellite image');const sid=state.eumet.mapSource,lid=state.eumet.mapLayer;if(m.getLayer?.(lid))m.removeLayer(lid);if(m.getSource?.(sid))m.removeSource(sid);m.addSource(sid,{type:'image',url,coordinates:[[-80,80],[80,80],[80,-80],[-80,-80]]});m.addLayer({id:lid,type:'raster',source:sid,paint:{'raster-opacity':['interpolate',['linear'],['zoom'],0,.90,4,.84,8,.56,11,0],'raster-contrast':.12,'raster-fade-duration':700}},beforeHazards(m));state.eumet.lastUrl=url;state.eumet.lastError=null;ingest({id:'meteosat',acquiredAt:null,retrievedAt:new Date().toISOString(),product:state.eumet.layer,pixelValidated:true,dimensions:dims,rendered:true,status:'TIMESTAMP_PENDING',truth:'COVERAGE_GAP'});return true}catch(e){state.eumet.lastError=String(e?.message||e);markGap('meteosat',state.eumet.lastError);return false;}}
function renderLedger(){const host=document.getElementById('map');if(!host)return;let el=document.getElementById('domAtmosphereTruth');if(!el){el=document.createElement('div');el.id='domAtmosphereTruth';el.style.cssText='position:absolute;left:10px;bottom:10px;z-index:9;max-width:min(78%,430px);padding:7px 9px;border-radius:10px;background:rgba(1,12,18,.82);border:1px solid rgba(111,231,255,.25);font:600 10px/1.35 system-ui;color:#d9fbff;pointer-events:none;backdrop-filter:blur(5px)';host.appendChild(el)}const observed=state.sources.filter(x=>x.truth==='OBSERVED').length,gaps=state.sources.filter(x=>x.truth!=='OBSERVED').map(x=>x.id);const daily=state.worldview.date?` · NASA VIIRS ${state.worldview.date} daily cloud/true-colour context`:'';el.textContent=`ATMOSPHERE ${observed}/${state.sources.length} source families timestamp-qualified${daily}${gaps.length?` · gaps: ${gaps.join(', ')}`:''}`;}
function ensureLayers(){if(!state.map)return;installWorldview();if(!state.map.getLayer?.(state.eumet.mapLayer))refreshEumet();renderLedger();}
function attachMap(m){if(!m)return;state.map=m;const go=()=>{ensureLayers();setTimeout(ensureLayers,350);setTimeout(ensureLayers,1200)};if(m.loaded?.())go();else m.once?.('load',go);try{m.on?.('styledata',()=>setTimeout(ensureLayers,120));m.on?.('idle',ensureLayers)}catch(_){}}
function findMap(){const m=window.DOMCurrentHazardMap?.map;if(m&&m!==state.map){attachMap(m);return true}if(m&&state.map===m){ensureLayers();return true}return false;}
function bind(){if(state.bound)return;state.bound=true;publish();findMap();let tries=0;state.attachTimer=setInterval(()=>{tries++;if(findMap()&&tries>20){clearInterval(state.attachTimer);state.attachTimer=0}else if(tries>120){clearInterval(state.attachTimer);state.attachTimer=0}},250);state.timer=setInterval(()=>{for(const s of state.sources){if(s.acquiredAt)Object.assign(s,freshness(s.acquiredAt,s.cadenceMin));}publish();findMap();if(state.map)refreshEumet();},600000);}
window.addEventListener('dom:map-ready',e=>attachMap(e.detail?.map));window.addEventListener('dom:satellite-observation',e=>ingest(e.detail));window.addEventListener('dom:satellite-coverage-gap',e=>markGap(e.detail?.id,e.detail?.reason));window.addEventListener('pagehide',()=>{if(state.timer)clearInterval(state.timer);if(state.attachTimer)clearInterval(state.attachTimer)},{once:true});
window.DOMLiveSatelliteObservationFabric=Object.freeze({bind,ingest,markGap,refreshEumet,installWorldview,sources:()=>state.sources.map(x=>({...x})),state:()=>({bound:state.bound,lastRefresh:state.lastRefresh,eumet:{...state.eumet},worldview:{...state.worldview},sources:state.sources.map(x=>({...x}))})});bind();
})();