[build]
builder = "NIXPACKS"

[deploy]
startCommand = "DOM_BROKER_PORT=$PORT python broker/dom_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 20
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
