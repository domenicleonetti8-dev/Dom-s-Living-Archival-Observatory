const fs=require('fs');
function read(p){return fs.readFileSync(p,'utf8')}
function assert(ok,msg){if(!ok){console.error('FAIL',msg);process.exitCode=1}else console.log('PASS',msg)}
const index=read('index.html');
const hazards=read('hazards.html');
const earth=read('earth.html');
const js=read('earth.js');
const ar=read('earth-ar-prediction-ui.js');
const audit=read('earth-source-audit.js');
const broker=read('broker/dom_observation_broker.py');
assert(index.includes('href="./hazards.html"')&&index.includes('href="./earth.html"'),'home exposes distinct hazard and Earth routes');
assert(!index.includes('href="./hazards.html#apple-ar"'),'Apple Earth no longer aliases hazard page');
assert(hazards.includes('href="./earth.html"')&&hazards.includes('OPEN GEOGRAPHIC EARTH'),'hazard dashboard routes Apple Earth outward');
assert(earth.includes('id="earthMap"')&&earth.includes('Google Maps overlay'),'Earth page exposes geographic map and optional Google provider path');
assert(earth.includes('id="toggleStorms"')&&earth.includes('id="toggleAirCurrents"'),'Earth page exposes side controls for obstructing atmospheric overlays');
assert(earth.includes('Pinpoint when the source supports it')&&earth.includes('Decimal display precision is not a claim'),'Earth page states location precision truth boundary');
assert(earth.includes('Surface view')&&!earth.includes('Walk / surface mode'),'web surface navigation is not mislabeled as a physical walk-through');
assert(earth.includes('station inventory rather than live measurement values'),'station inventory is explicitly separated from measurement liveness');
assert(js.includes("style:'https://demotiles.maplibre.org/globe.json'")&&js.includes("map.setProjection({type:'globe'})"),'renderer uses geographic globe projection');
assert(js.includes('validLatLon')&&js.includes("coordinates:[r.lon,r.lat]"),'observations are surface-anchored from validated longitude/latitude');
assert(js.includes("locationPrecision:safeText(r.locationPrecision||'unresolved')")&&!js.includes("locationPrecision:safeText(r.locationPrecision||'source-coordinate')"),'generic Earth point ingestion defaults unknown precision to unresolved rather than source-coordinate');
assert(js.includes('earthquake.usgs.gov')&&js.includes('eonet.gsfc.nasa.gov')&&js.includes('ndbc.noaa.gov')&&js.includes('tidesandcurrents.noaa.gov')&&js.includes('api.weather.gov'),'multi-source live/current product fabric is wired');
assert(js.includes("SOURCE_GEOMETRY_TYPES=new Set(['Polygon','MultiPolygon','LineString','MultiLineString'])"),'Earth renderer accepts authoritative area and track geometry without inventing shape types');
assert(js.includes("'dom-event-areas'")&&js.includes("'dom-event-area-fill'")&&js.includes("'dom-event-area-line'")&&js.includes("'dom-event-track-line'"),'Earth renderer has dedicated authoritative polygon and track layers');
assert(js.includes("geometryRole:role")&&js.includes("placement:'source-geometry'"),'source geometry preserves explicit geometry role and source placement semantics');
assert(js.includes("r.geometryRole||'source-geometry'")&&js.includes('geometry:r.geometry'),'broker-supplied authoritative geometry is consumed by the Earth renderer');
assert(js.includes("'forecast-track'")&&js.includes("'best-track'")&&js.includes("'forecast-cone'"),'storm rendering has explicit forecast track, best track and cone semantics');
assert(js.includes('function visibleRecords()')&&js.includes("records.filter(r=>r.overlayClass!=='storm')"),'storm-off state removes storm points before MapLibre clustering');
assert(js.includes("map?.getSource('dom-observations')?.setData(toGeoJSON())")&&js.includes('updateGoogleMarkers()'),'storm visibility changes immediately rebuild clustered source and Google markers');
assert(js.includes('horizontalAccuracyMeters')&&js.includes('uncertaintyRadiusMeters')&&js.includes('confidenceLevel')&&js.includes('uncertaintyBasis'),'Earth record model carries source-supplied positional uncertainty metadata');
assert(js.includes('source did not publish a numeric horizontal uncertainty'),'Earth popup reports missing numeric uncertainty instead of inferring it');
assert(ar.includes('horizontalAccuracyMeters')&&ar.includes('uncertaintyRadiusMeters')&&ar.includes('confidenceLevel')&&ar.includes('uncertaintyBasis'),'AR scene contract preserves positional uncertainty metadata');
assert(ar.includes('Missing accuracy fields remain null rather than inferred'),'AR export states uncertainty null behavior truthfully');
assert(js.includes('stormAware')&&js.includes('toggleStorms')&&js.includes('AIR_CURRENT_LAYER_IDS')&&js.includes('toggleAirCurrents'),'storm and air-current obstruction controls are wired');
assert(js.includes('api.open-meteo.com/v1/ecmwf')&&js.includes('wind_speed_10m,wind_direction_10m')&&js.includes('forecast_hours=3'),'Earth requests timestamped ECMWF IFS 10 m wind vectors through Open-Meteo');
assert(js.includes("'dom-air-current-streamlines'")&&js.includes("'dom-air-current-arrows'")&&js.includes("'dom-air-currents'"),'timestamped wind vectors have dedicated source, line and arrow layers');
assert(js.includes("modelNature:'forecast-model-field'")&&js.includes('Vector length is a visual magnitude encoding')&&earth.includes('forecast-model output, not a direct global anemometer network'),'wind layer is labeled as model output and does not masquerade as direct observation or trajectory');
assert(js.includes('meteorological wind from')&&js.includes('flow toward'),'wind popup distinguishes meteorological from-direction from displayed flow direction');
assert(js.includes('sourceCellLatitude')&&js.includes('sourceCellLongitude')&&js.includes('requestedSampleLatitude')&&earth.includes('renders that returned grid coordinate'),'wind renderer preserves returned model-cell coordinates separately from requested display samples');
assert(js.includes('windCellKeys')&&js.includes('unique model-grid samples'),'wind renderer deduplicates repeated model-grid cells');
assert(js.includes('windTimeOffsetMinutes')&&js.includes("'STALE MODEL'")&&earth.includes('STALE MODEL'),'wind field timing is compared to browser time and stale model output is labeled explicitly');
assert(js.includes('authoritative source geometries')&&js.includes('without geometry'),'NWS Earth status distinguishes rendered source geometry from unlocated alerts');
assert(js.includes("getJSON(`${root}/v1/sources`")&&js.includes("x.status==='stale'")&&js.includes("x.status==='error'"),'Earth broker panel consumes active/stale/error source health');
assert(broker.includes('"nhc-tropical"'),'NHC tropical source is part of canonical broker source health registry');
assert(!/Math\.random\s*\(/.test(js),'Earth renderer does not randomly scatter sensor positions');
assert(audit.includes("DIRECT_BROWSER=new Set(['usgs-eq','nasa-eonet','nws-alerts'])"),'source audit limits direct-browser measurement claims to feeds actually fetched');
assert(audit.includes("INVENTORY_ONLY=new Set(['ndbc-stdmet','ndbc-ocean','ndbc-waterlevel'])"),'NDBC measurement families are explicitly marked inventory-only on the Earth audit');
assert(audit.includes("label:'LIVE'")&&audit.includes("label:'STALE'")&&audit.includes("label:'FAILED'")&&audit.includes('/v1/sources'),'source audit exposes broker live/stale/failed states when configured');
assert(audit.includes('LIVE/STALE/FAILED come from the configured broker'),'source audit explains runtime truth semantics');
if(process.exitCode)process.exit(process.exitCode);